{
  "name": "kagestream",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"PORT=4000 npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:anistream": "concurrently -k -n backend,frontend -c blue,green \"PORT=4000 PROVIDER=anistream npm run dev -w backend\" \"npm run dev -w frontend\"",
    "seed": "npm run seed -w backend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
